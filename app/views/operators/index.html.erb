<h1>Operadores</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Codigo</th>
    <th></th>
  </tr>

<% @operators.each do |operator| %>
  <tr>
    <td><%= operator.name %></td>
    <td><%= operator.code %></td>
    <td><%= link_to 'Ver', operator %></td>
  </tr>
<% end %>
</table>

<br />
